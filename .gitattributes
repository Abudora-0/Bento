# Normalise line endings. Without this the two mirrored type files differ by
# CRLF alone, which buries a real divergence in noise and makes every commit
# print a wall of warnings on Windows.
* text=auto eol=lf

# Generated by scripts/make-icon.mjs, never hand edited.
*.png binary
